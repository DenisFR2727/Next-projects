{
    "compilerOptions": {
        "baseUrl": ".", // Обов'язково! Базовий шлях — корінь проєкту
        "paths": {
            "@styles/*": ["app/styles/*"], // або src/styles/*, якщо використовуєш src
            "@/*": ["app/*", "./*"]
        }
    }
}
